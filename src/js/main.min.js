let timerInterval;const secondsPerMinutes=60,goalMinutes=40;let goalSeconds;const goalExtra=20;let extraSeconds,progressBarProgress,allSeconds,currentValuePercent=0,minutes=39,seconds=53;const redColor="#ee796e",greenColor="#4d945b",timerFull=document.querySelector(".timer__full"),timerProgress=document.querySelector(".timer__progress"),timerExtra=document.querySelector(".timer__extra"),playButton=document.querySelector(".btn__play"),minutesNode=document.querySelector(".time__minutes"),secondsNode=document.querySelector(".time__seconds"),time=document.querySelector(".time"),fullProgressValue=parseInt(timerFull.getAttribute("stroke-dasharray"));function startTimer(){playButton.style.display="none",time.style.display="block",goalSeconds=goalMinutes*secondsPerMinutes,updateTimerDisplay(),updateProgressBar(),timerInterval=setInterval((function(){seconds<59?seconds+=1:(seconds=0,minutes+=1),40===minutes&&startExtraTimer(),updateProgressBar(),updateTimerDisplay()}),1e3)}function updateTimerDisplay(){minutesNode.innerHTML=String(minutes).padStart(2,"0"),secondsNode.innerHTML=String(seconds).padStart(2,"0"),document.title=`${String(minutes).padStart(2,"0")} : ${String(seconds).padStart(2,"0")}`}function updateProgressBar(){allSeconds=60*minutes+seconds,currentValuePercent=(allSeconds/goalSeconds*100).toFixed(),progressBarProgress=(fullProgressValue-fullProgressValue*(currentValuePercent/100)).toFixed(),timerProgress.setAttribute("stroke-dashoffset",progressBarProgress+"px")}function updateProgressExtra(){extraSeconds=60*minutes+seconds-goalMinutes*secondsPerMinutes,goalSeconds=goalExtra*secondsPerMinutes,currentValuePercent=(extraSeconds/goalSeconds*100).toFixed(),progressBarProgress=(fullProgressValue-fullProgressValue*(currentValuePercent/100)).toFixed(),timerExtra.setAttribute("stroke-dashoffset",progressBarProgress+"px")}function startExtraTimer(){clearInterval(timerInterval),timerExtra.style.display="block",goalSeconds=goalExtra*secondsPerMinutes,timerInterval=setInterval((function(){seconds<59?seconds+=1:(seconds=0,minutes+=1),41===minutes&&stopTimer(),updateProgressExtra(),updateTimerDisplay()}),1e3)}function stopTimer(){playButton.style.display="block",time.style.display="none",seconds=0,minutes=0,clearInterval(timerInterval)}playButton.addEventListener("click",startTimer);